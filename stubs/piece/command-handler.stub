<?php

declare(strict_types=1);

namespace {{ namespace }};

use {{ commandNamespace }}\{{ commandClass }};
use {{ busNamespace }}\Command;
use {{ busNamespace }}\CommandHandler;

final class {{ class }} implements CommandHandler
{
    public function handle(Command $command): mixed
    {
        assert($command instanceof {{ commandClass }});

        // TODO: implementer la logique metier.

        return null;
    }
}
